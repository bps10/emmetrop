cmake_minimum_required (VERSION 2.6)
project (SchematicEye)

include_directories ("${PROJECT_BINARY_DIR}")

include_directories ("C:\Cplusplus\goptical-1.0\goptical_core\src")
add_subdirectory (src)

set (EXTRA_LIBS ${EXTRA_LIBS} libgoptical)

# add the executable
add_executable (SchematicEye SchematicEye.cxx)
target_link_libraries (SchematicEye  ${EXTRA_LIBS})


# add the main library
add_library(MathFunctions mysqrt.cxx ${CMAKE_CURRENT_BINARY_DIR}/Table.h)
 
install (TARGETS libgoptical DESTINATION bin)
install (FILES libgoptical.la DESTINATION include)


# add the install targets
install (TARGETS SchematicEye DESTINATION bin)

